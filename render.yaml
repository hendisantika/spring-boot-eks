services:
  - type: web
    name: spring-boot-api
    env: docker
    plan: free
    buildCommand: ./mvn clean package -DskipTests
    startCommand: java -jar target/eks-0.0.1.jar
    docker:
      dockerfilePath: Dockerfile